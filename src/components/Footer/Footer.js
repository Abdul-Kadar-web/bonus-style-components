import React from 'react';
import styles from  './Footer.module.css'

const Footer = () => {
    return (
        <div>
            <p><small className={styles.title}>This is Footer.</small></p>
        </div>
    );
};

export default Footer;